# 0.2 Interview Starter: Envelope Budgeting

A minimal two-file full-stack application demonstrating the core Zauberflöte DSL.
- Backend: `lib/envelope_app/app.ex`
- Frontend: `priv/static/index.html`

## Run

```bash
mix deps.get
mix run --no-halt
```

Open `http://localhost:4010`.
