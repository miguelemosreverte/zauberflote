# example-01-http-ui-sql-bare-minimum

Port: 4001

Two-file full-stack app:
- Backend: `lib/envelope_app/app.ex`
- Frontend: `priv/static/index.html`

## Run

```bash
mix deps.get
mix run --no-halt
```

Open `http://localhost:4001`.
