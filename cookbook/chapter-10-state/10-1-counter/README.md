# 10-1 Counter

Simple counter demonstrating basic client-side state with `.local()`, `.set()`, and `.adjust()`.

Port: 5001

## Features
- Increment/decrement buttons
- Reset to zero
- Set custom value
- All state managed client-side
